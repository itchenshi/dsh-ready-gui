# 🚀 DSH Ready GUI v0.6.1

> DeepSeek Harness 桌面壳 —— 内嵌 Web UI、自动保持最新引擎、自带数据目录管理与系统托盘。

**发布说明：** 这一版修掉一个**会让人以为插件没装好**的问题（Command Code 余量报「未配置密钥」），
并给内置插件的自动更新补上提示。

---

## 🔑 修：Command Code 余量显示「未配置密钥」

**症状**：把数据目录切到另一个位置（例如改成「跟随系统」）后，**即便卸载再重装**模型余量插件，
会话标题旁也不显示 Command Code 的余量，只显示 **「未配置密钥」** —— 而同一个密钥拿来做对话完全正常。

**原因**：插件找密钥用的是**写死的名字** `COMMANDCODE_GOAT_API_KEY`（当年那条路由叫
`commandcode-goat`）。而引擎是按**路由自己声明的 `apiKeyEnv`** 取密钥的。换数据目录后路由名变成
`commandcode`（这正是本插件补丁声明的名字），GUI 为它推导出的密钥名是 `COMMANDCODE_API_KEY`
—— 于是「引擎取得到、插件取不到」，对话正常、余量报无密钥。重装插件当然没用：那个名字在插件的
默认值里，不在你的配置里。

**修法**：插件现在**跟着路由走** —— 先读该路由自己声明的 `apiKeyEnv`，找不到才回退到插件默认名。
三个分区（OpenCode Go / Command Code / DeepSeek）都受益；找不到密钥时的诊断也改为列出它找过的
**所有**名字，而不是只报一个猜的。随包版本 `dsh-model-surplus 0.4.0 → 0.4.1`。

**实测**（真实数据）：路由 `commandcode` / `apiKeyEnv=COMMANDCODE_API_KEY`，
查找顺序 HIT `COMMANDCODE_API_KEY` → miss `COMMANDCODE_GOAT_API_KEY`，
额度接口 HTTP 200、`credits=63.10`、含窗口数据。

---

## 🔔 新：内置插件更新时会告诉你

此前随包插件版本更高时，启动维护会**自动**把它换掉 —— 但这件事**只进日志**，用户完全不知道自己的
插件被动过；反倒「启动失败自动剔除」那条路径有提示，等于只有坏消息会说话。

现在更新成功后会弹右下角常驻角标：**「内置插件已更新：模型余量 v0.4.1（本次启动已生效）」**，
只列**真正变了**的插件。三个细节：

- **只报变化**：首次安装不会误报「已更新」（全新环境四个插件都会走安装路径，报成"更新"会让人困惑）；
- **只报已生效**：提示挂在引擎**成功就绪之后** —— 装插件发生在引擎启动之前，所以此时已经生效、
  不需要重启；而如果新插件把引擎搞崩，走的是「插件导致启动失败」那条更重要的提示；
- **不抢占更重要的消息**：与「引擎已更新」共用同一个角标，当前已有角标时不覆盖；
  **切换数据目录后重新允许提示**（换了环境就是换了插件版本，不该静默）。

---

## 🛠 安全 / 逻辑审计的修复

对 GUI 与四个插件做了一轮审计（命令执行、路径拼接、IPC 面、导航围栏、密钥泄露、插件客户端
注入点、插件 HTTP 路由围栏、临时文件并发）。**没有发现重大安全漏洞** —— Electron 三个窗口一律
`contextIsolation` + `sandbox`、13 个特权 IPC 全部校验发送者身份、两个 preload 都窄、导航围栏按
精确文件路径比较、子进程一律参数数组不经 shell、插件路由都走引擎信任围栏。修掉的是这些：

- **并发写**：应用更新缓存的临时文件名只带 pid，而同一进程里有两个写者（后台检查有闸、用户点
  「立即更新」的那条**不在闸内**），撞上时一方 rename 发布的会是另一方的字节 → 加随机段。
- **每次设置变更都打一次上游**：模型余量插件的 Command Code 目录接口在每次 `llm-pi-ai` 设置变更时
  都会请求（主题/语言也属于设置）→ 加 TTL 记忆（成功 10 分钟、失败 60 秒）。
- **校验写法不一致**：`settings:autosize` 自己判发送者，与其它 12 个 handler 的写法不同 ——
  下次审计会被漏掉（这次就漏了）→ 统一。
- **构建脚本**：Electron 下载的临时文件名固定，两个构建并行会互相覆盖 → 加随机段。
- **文档**：`preload.js` 注释写着「主窗口没有 preload」，实际它有（权限更窄的
  `workspace-preload.js`）→ 改正并说明差异。
- **公共面收缩**：`plugin-manager` 有 10 个内部函数被导出却**没有任何外部引用**（不是死代码，
  但不该对外暴露）→ 移除。

## 🧪 测试

- `verify:builtin`（真实引擎）新增两幕：**旧包名环境整体换成新包**（摘 bundles + dependencies、
  交接替代条目、清 staging 拷贝、不重复登记），以及**四个随包插件全部降级后一次性更新**
  + 「已装版本更高绝不降级」+ 「非随包的市场插件不受外壳版本管理」。
- `engine-patch` 补上 `injectedState` 的 6 条断言（它是「兜底补丁当前状态」的唯一判据，此前只有
  生产调用、没有用例），含「strip 之后计数归零」这一让再次注入安全的**不变量**。
- `plugin-enable` 17 → 18 项；`plugin-manager` 新增 2 项；`dsh-model-surplus` 33 项。

## ⬆️ 升级说明

- **无配置迁移、无需手动操作**：数据目录、模型配置、会话历史原样保留。
- 启动时会自动把 `dsh-model-surplus` 换到 0.4.1，并弹出「内置插件已更新」角标。
- **Command Code 用户**：如果你的路由名是 `commandcode`，以前显示的「未配置密钥」会变成真实余量，
  **不需要重新配密钥** —— 密钥一直都在，只是插件找错了名字。

## 📦 下载

- **GitHub（安装包在这里）**：https://github.com/itchenshi/dsh-ready-gui/releases/tag/v0.6.1
- **Gitee**：https://gitee.com/itchenshi/dsh-ready-gui/releases/tag/v0.6.1
- **GitCode**：https://gitcode.com/itchenshi/dsh-ready-gui/releases/tag/v0.6.1

三个平台都有 v0.6.1 的 Release 页与更新说明；**安装包只在 GitHub**（Gitee / GitCode 的 Release 提供
源码包）。原因有两条：

- 本项目要同时产出 Windows 安装包、macOS（x64 + arm64）的 .dmg 和 Linux 的 AppImage，只有 GitHub
  Actions 能一次提供 Windows / macOS / Linux 三种 runner 做矩阵构建（Gitee Go 的云端构建只有 Linux
  容器，Node 版本也只到 15，够不上本项目要求的 Node ≥ 23）。
- Gitee 免费版限制**附件单文件 100MB、单仓库附件共 1GB**，而我们的产物是 133–194MB、合计约 1.6GB
  —— 一个都传不上去。

国内网络下载慢的话，可以用镜像加速或代理，安装包本身与源码是同一份。

## ⚠️ 平台验证范围（如实说明）

- **Windows**：我在用，实测过。
- **macOS / Linux**：每次发版都会由 CI 的四平台矩阵构建出 `.dmg`、`.AppImage` 等产物，
  但**我手上没有这两台机器，未做实机验证** —— 所以这里只敢说「已构建」，不敢说「支持」。
  你在 macOS 或 Linux 上跑过的话，欢迎开 issue 告诉我结果。
