# 🚀 DSH GUI v0.4.0

> DeepSeek Harness 桌面壳 —— 内嵌 Web UI、自动保持最新引擎、自带数据目录管理与系统托盘。

**发布说明：** 自 v0.3.0 以来的功能版本。两个 OpenCode 插件合并为「OpenCode Go 增强」并自动维护 DeepSeek V4.1 模型；用量插件新增**当前选中模型的每月额度上限**并会**自动从官方文档更新额度表**；设置窗口的第三方插件区更紧凑、描述更简短。

---

## ✨ 主要更新

### 🔧 插件合并：「OpenCode Go 增强」（dsh-opencode-go）

原 `dsh-opencode-go-session` 与 `dsh-opencode-go-api` **合并为一个插件**，一次解决 OpenCode / OpenCode Go 路由的三件事：

- **声明路由协议**：给 `opencode-go` 路由补 `api: openai-completions`，修掉目录外模型（如 `deepseek-v4.1-flash`）的
  `needs an api` 报错，以及模型页「添加模型」被拒的问题。
- **自动补 DeepSeek V4.1 模型**（新）：引擎启动后检查——**存在 `opencode-go` 路由且模型列表里还没有 `deepseek-v4.1-*` 时自动追加**
  （走与模型页相同的写入路径，幂等；路由不存在则不动）。
- **附加会话头**：为发往 OpenCode 的请求附按会话 `x-opencode-session` 头，修复 400 MissingSessionID；默认用**不透明 UUID**，绝不把内部会话 ID 发给第三方。

> **升级平滑**：装过任一旧插件的用户，首次启动会自动摘除旧包、装上新插件，并**沿用你原来的启用/禁用选择**；旧补丁层的遗留行也会被清理，不会新旧两版同时加载。

### 📊 用量插件：显示选中模型的每月上限 + 自动更新额度表

「模型用量与余量」（`dsh-model-usage` 0.3.0）在原有账户级用量/余额之外新增：

- **选中模型的每月额度上限**：会话标题右侧在「滚动 / 周 / 月」百分比之后显示 `上限 $60` 一类的标签。
- **悬停看三段额度**：该模型的 **5 小时 / 周 / 每月** 额度（按官方「5 小时 = 月额度 20%、每周 = 50%、每月 = 100%」规则换算），
  并标注数据来源与「**百分比是账户整体用量，上限按当前选中模型**」——两者含义不同，不会看混。
- **额度表自动更新**：OpenCode Go 官方**没有**提供每模型额度的接口，插件因此内建一份额度表（28 个模型），
  并在启动时**自动抓取官方文档页解析更新**（公开页面、不需要 API 密钥），结果缓存本地；
  抓取或解析失败会回退到上次缓存、再回退到内置表，不会报错也不会空白。
- **新增限流提示**：账户达到每月上限（上游返回 `rate-limited`）时，上限标红并在悬停里给出重置时间。

### 🎛 设置窗口：第三方插件区更清爽

- **布局优化**：插件标题加粗成行，描述最多显示两行、超出自动省略，行距更紧凑。
- **描述精简**：全部插件的中英文描述都改为一句话（例如「OpenCode Go 增强」由 151 字精简到 49 字），
  设置窗口不再被大段说明撑开。

---

## 📦 下载

| 平台 | 安装包 | 便携版 |
|---|---|---|
| **Windows** | `DSH GUI Setup 0.4.0.exe`（NSIS 安装包） | `DSH GUI-0.4.0-win.zip`（便携） / `DSH-GUI-WIN.zip`（解压即用目录） |
| **macOS** | `DSH GUI-0.4.0-arm64.dmg`（Apple Silicon） / `DSH GUI-0.4.0.dmg`（Intel） | |
| **Linux** | `DSH GUI-0.4.0.AppImage` | `DSH-GUI-LINUX.zip` |

> 提示：各平台安装包均已捆绑便携 Node（v26），终端用户无需安装任何运行时。
> 三平台仓库（GitHub / Gitee / GitCode）互为镜像；**安装包只挂在 GitHub Releases**
> —— Gitee 单附件上限 100 MB、GitCode 不支持附件，因此这两个镜像只承载源码与发布说明。

### 升级说明

- 直接覆盖安装即可，数据目录与会话记录不受影响。
- 若你在 v0.3.0 装过「OpenCode 会话头」或「OpenCode Go 路由协议」插件：首次启动会自动合并换成
  「OpenCode Go 增强」，无需手动操作。
- 用量插件会自行维护额度表：首次启动会联网抓一次官方文档（约 1 秒，失败不影响使用）。

> **文件名说明**：GitHub 会把附件名里的空格替换成点号，所以页面上的文件是
> `DSH.GUI.Setup.0.4.0.exe`、`DSH.GUI-0.4.0-win.zip` 这种形式（本地构建产物为空格版）。
> 内容完全一致，按页面上的名字下载即可。

---

## 🧭 运行方式

```bash
# 源码运行（需 Node.js ≥ 23）
npm install
npm start
```

首次启动自动安装 DeepSeek Harness 引擎（约 1–2 分钟）。

---

## 🙏 反馈

- 问题与建议：GitHub Issues
- 项目地址：https://github.com/itchenshi/DeepSeekHarnessGUI

*完整改动见 [CHANGELOG.md](CHANGELOG.md)。*
