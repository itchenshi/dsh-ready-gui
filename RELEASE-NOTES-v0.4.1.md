# 🚀 DSH GUI v0.4.1

> DeepSeek Harness 桌面壳 —— 内嵌 Web UI、自动保持最新引擎、自带数据目录管理与系统托盘。

**发布说明：** 这是一次**安全与可靠性修复版本**。起因是两轮针对「GUI + 四个内置插件」的安全审计：修掉了一批围栏缺失与鉴权缺失问题，其中一条（插件注册的浏览器路由没有鉴权）会导致**本机任何进程、以及被 DNS rebinding 的网页**都能读到你的账户用量与余额；另有一条会让**任意第三方插件把引擎自己的行禁用掉**。同时新增「DSH GUI 自身自动检查新版本」与「输入框快捷键」插件。

---

## 🔐 安全修复（建议所有用户升级）

- **插件浏览器路由此前完全没有鉴权**（`dsh-model-usage` / `dsh-composer-keys` / `dsh-gui-last-session`）。
  实测：不带任何凭据的 `GET /model-usage` 会返回**账户用量与 DeepSeek 余额**；`POST /composer-keys`
  会**真的写入设置**；带上 `Host: evil.example` 也一样（DNS rebinding 形状）。
  现在统一走引擎自己的信任围栏（Host 白名单 + 浏览器会话 cookie），拿不到围栏时 fail-closed。
  合法的页面请求不受影响（同源自动带 cookie）。
- **主窗口缺少导航围栏**：引擎页里的任何脚本（包括第三方插件的页面半边）此前能把
  **带着 preload 桥的窗口**导航到任意外部页面，从而拿到「重启引擎」「读写上次会话」的能力。
  现在只允许停留在已验证的引擎来源，弹窗一律不托管（http(s) 交给系统浏览器）。
- **引擎 URL 不再无条件信任**：`dsh web: …` 那行来自引擎进程的 stdout，插件能在服务真正监听前
  打印自己的 URL。现在只接受 http + 回环/私有 IP 字面量，域名一律拒绝。
- **权限默认全放行**：Electron 默认批准所有权限请求，此前引擎页／插件页面半边可**静默打开麦克风、
  摄像头、读取定位、刷通知**。现在默认拒绝，只放行剪贴板写入、全屏、指针锁定。
- **任意第三方插件可禁用引擎自己的行**：插件声明 `dsh.bundle.patch: "../../…"`（越界到别的补丁文件）
  或额外塞一个**引擎根本不会加载**的 `cordis.patch.yml`，就能把 `webserver` / `modules` / `session`
  之类的引擎行 id 认领成自己的，随后由 GUI「名正言顺」地写出 `disabled: true` 并返回成功。
  现在只读引擎真正会加载的那一个补丁文件，且必须落在包目录内；另有保护名单与跨包归属校验。
- **特权 IPC 统一校验发送方**（此前 13 个通道里只有 1 个校验）；**断开时的引擎 token 不再写入
  启动失败日志**；破坏性测试钩子仅在未打包构建生效。

## 🛠 可靠性修复

- **补丁层跨进程丢行**：GUI 与插件市场会写同一个 `cordis.patch.yml`，此前用固定 `.tmp` 名且没有
  锁 —— 实测 3 个进程并发写 40 行会**丢掉 17–28 行却仍报成功**。现在有跨进程锁、唯一临时名、
  写后校验与重试。
- **写失败不再翻转市场状态**：此前补丁层写失败时仍会把「已禁用」写进市场状态，界面显示已禁用、
  引擎照常加载（正是这类漂移最容易让人以为插件坏了）。
- **补丁层损坏会被拒绝而不是接受**：空占位后还有内容、缩进垃圾等形状此前会被当成合法并继续追加。
- **安装/更新**：登记在案但文件已丢的插件现在会真的重装（此前点多少次都是「失败：?」）；
  随包版本比已装旧时才重装（此前会把更新的副本无人值守降级）；暂存复制不再跟随软链；
  手动更新引擎前先停引擎；npm 安装加 10 分钟超时。
- **数据目录切换**：修复「移动并切换」后引擎不会重启的回归；确认框被取消时不再闪「已保存」。
- 引擎页「回到最近会话」的兜底补丁锚点已适配当前引擎（此前因函数签名多了参数而**从未生效**）。

## ✨ 新功能

- **自动检查 DSH GUI 新版本**：启动后自动查（12 小时内不重复），有新版用通知窗口提醒，离线静默。
  检查**自动兼顾国内外网络** —— 同一个版本同时发布于 GitHub / Gitee / GitCode，国内网络
  （`zh-CN` 或 Asia/Shanghai 等时区）先试 Gitee → GitCode → GitHub，国外反之；每个源单独限时、
  逐个回退，并记住上次可用的源优先使用。**无需任何配置**；手动入口是托盘「检查 DSH GUI 更新…」，
  会打开实际答上来的那个平台的下载页。（实测本机 Gitee 首次尝试 251ms 命中。）
- **输入框快捷键插件（`dsh-composer-keys`）**：在设置窗口「通用」页配置 Enter / Shift+Enter /
  Ctrl+Enter 是「发送消息」还是「换行」。默认即引擎默认，不改动任何现有习惯；实现上是转派引擎
  自己的按键事件，因此不会与编辑器行为不一致。

## 🎛 顺带

- 设置窗口的第三方插件列表更紧凑：通用规则写在段落说明里，因插件而异的
  「含页面部分 · 需刷新页面」以小标签标在对应行上。
- 插件状态指纹补上页面半边与补丁行 id —— 这两项变化此前不会触发设置窗口重绘。

---

## ⬆️ 升级说明

- **插件版本号已提升**（`dsh-model-usage` 0.3.1、`dsh-gui-last-session` 0.1.1、
  `dsh-opencode-go` 0.1.1、`dsh-composer-keys` 0.2.0）。GUI 的「随包更新」只在版本变化时才替换
  已装副本，因此**必须使用 v0.4.1 及以后的新构建**，这些安全修复才会真正进入你的 profile。
  装好新版本后首次启动会自动替换；也可以在新版设置窗口点「修复 / 重试」立即触发。
- 无配置迁移、无需手动操作；数据目录与既有设置原样保留。

## 📦 下载

- GitHub：https://github.com/itchenshi/DeepSeekHarnessGUI/releases/latest
- Gitee（国内推荐）：https://gitee.com/itchenshi/DeepSeekHarnessGUI/releases
- GitCode（国内备用）：https://gitcode.com/itchenshi/DeepSeekHarnessGUI/releases

## 🙏 致谢

本轮修复来自一次针对 GUI 与四个内置插件的系统审计（含可复现的攻击验证）。感谢每一位反馈问题的用户。
